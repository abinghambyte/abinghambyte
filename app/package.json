{
  "name": "clearline",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Clearline Phase-1 seed: the safety-critical check-in loop, driven by a simulated phone line (no telephony, no cost).",
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "demo": "node --disable-warning=ExperimentalWarning --experimental-strip-types src/demo/run.ts",
    "test": "node --disable-warning=ExperimentalWarning --experimental-strip-types --test src/domain/interpret.test.ts src/worker/check.test.ts src/telephony/twiml.test.ts src/telephony/twilio.test.ts",
    "typecheck": "tsc --noEmit"
  }
}
